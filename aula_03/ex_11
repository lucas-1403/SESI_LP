#include <stdio.h>

void main() {
    int N1;
    int mult = 1;

    printf("Digite um numero: ");
    scanf("%i", &N1);

    int i = N1;

    while (i > 0) {
        mult *= i;
        i--;
    }

    printf("O fatorial de %i é: %i\n", N1, mult);

}